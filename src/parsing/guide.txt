1. Validate the file — does it exist, can you open it, does it end in .cub ✓

2. Read the file — into lines you can process ✓

3. Parse the elements — NO/SO/WE/EA paths, F and C colors ✓
	-identify each directive ✓
	-store its value ✓
	-reject duplicates ✓
	-reject missing identifiers ✓
	-reject malformed values ✓
	-stop when the map begins ✓

4. Parse the map — the grid at the end of the file
	-normalize the map

5. Validate the map — closed by walls, exactly one player, only valid characters
	-...
